# gas-mcp-server

Claude/AI 에이전트가 Google Workspace(Sheets, Docs, Drive, Gmail, Calendar)를 **MCP(Model Context Protocol)** 방식으로 제어할 수 있게 하는 Google Apps Script 웹앱입니다.

## 지원 액션

| 서비스 | 액션 | 설명 |
|--------|------|------|
| Sheets | `sheets.read` | 스프레드시트 범위 읽기 |
| Sheets | `sheets.write` | 스프레드시트 범위 쓰기 |
| Sheets | `sheets.append` | 시트 끝에 행 추가 |
| Docs | `docs.read` | 문서 텍스트 읽기 |
| Docs | `docs.create` | 새 문서 생성 |
| Drive | `drive.list` | 폴더 내 파일 목록 |
| Drive | `drive.upload` | 파일 업로드 |
| Gmail | `gmail.send` | 이메일 발송 |
| Gmail | `gmail.search` | 이메일 검색 |
| Calendar | `calendar.list` | 일정 조회 |
| Calendar | `calendar.create` | 일정 생성 |

## 설치 방법

### 1. Google Apps Script 프로젝트 생성

1. [script.google.com](https://script.google.com) → **새 프로젝트**
2. 기본 `Code.gs` 내용을 이 저장소의 `Code.gs`로 교체
3. **파일 추가** → 스크립트 → `Config` → `Config.gs` 내용 붙여넣기
4. `appsscript.json` 편집: 메뉴 **프로젝트 설정** → **편집기에서 appsscript.json 표시** 체크 → 내용 교체

### 2. API 키 설정

`Config.gs`에서 `API_KEY`를 안전한 랜덤 문자열로 변경:

```javascript
API_KEY: 'your-secure-random-key-here',
```

### 3. 웹앱 배포

1. **배포** → **새 배포** → 유형: **웹 앱**
2. 실행 주체: **본인**, 액세스: **모든 사용자**
3. 배포 URL 복사 (이것이 MCP 엔드포인트)

## 사용법

### 요청 형식

```bash
curl -X POST "YOUR_DEPLOYMENT_URL" \
  -H "Content-Type: application/json" \
  -d '{
    "apiKey": "your-api-key",
    "action": "sheets.read",
    "params": {
      "spreadsheetId": "1BxiMVs0XRA5nFMdKvBdBZjgmUUqptlbs74OgVE2upms",
      "range": "Sheet1!A1:D10"
    }
  }'
```

### 응답 형식

```json
{
  "status": 200,
  "ok": true,
  "data": {
    "values": [["이름", "수량", "가격"], ["위젯A", 100, 5000]]
  }
}
```

### 주요 액션 파라미터

#### sheets.append
```json
{
  "action": "sheets.append",
  "params": {
    "spreadsheetId": "...",
    "sheetName": "재고",
    "values": [["SKU-001", 50, 12000]]
  }
}
```

#### gmail.send
```json
{
  "action": "gmail.send",
  "params": {
    "to": "supplier@example.com",
    "subject": "발주 요청",
    "body": "첨부 목록대로 발주 부탁드립니다.",
    "htmlBody": "<h1>발주 요청</h1><p>상세 내용...</p>"
  }
}
```

#### calendar.create
```json
{
  "action": "calendar.create",
  "params": {
    "title": "프로모션 미팅",
    "startTime": "2026-03-01T14:00:00+09:00",
    "endTime": "2026-03-01T15:00:00+09:00",
    "location": "온라인"
  }
}
```

## 보안

- **API_KEY**: 모든 요청에 `apiKey` 필드 필수
- **ALLOWED_ORIGINS**: 특정 도메인만 허용 가능 (빈 배열 = 제한 없음)
- **서비스 ON/OFF**: `Config.gs`의 `SERVICES`에서 불필요한 서비스 비활성화
- 웹앱 배포 시 "모든 사용자" 접근이지만, API 키로 실질적 접근 제어

## 🛒 이커머스 활용 예시

### 1. AI 에이전트가 재고 시트 자동 업데이트

```json
{
  "apiKey": "...",
  "action": "sheets.write",
  "params": {
    "spreadsheetId": "재고관리_시트_ID",
    "range": "재고!B2:C2",
    "values": [[150, "2026-02-17T14:00:00"]]
  }
}
```

AI가 판매 데이터를 분석하고, 실시간으로 재고 수량과 최종 업데이트 시간을 시트에 기록합니다.

### 2. 경쟁사 가격 데이터를 시트에 기록

```json
{
  "apiKey": "...",
  "action": "sheets.append",
  "params": {
    "spreadsheetId": "가격모니터링_시트_ID",
    "sheetName": "경쟁사가격",
    "values": [["2026-02-17", "경쟁사A", "위젯X", 4500, "쿠팡"]]
  }
}
```

크롤링한 경쟁사 가격을 자동으로 시트에 누적하여 가격 추이를 분석합니다.

### 3. 프로모션 성과 리포트 자동 생성

```json
{
  "apiKey": "...",
  "action": "docs.create",
  "params": {
    "title": "2026년 2월 프로모션 성과 리포트",
    "body": "■ 기간: 2/1~2/14\n■ 매출: 15,200,000원 (+23%)\n■ 주문수: 1,847건\n■ 베스트: 위젯A (432건)\n\n상세 분석..."
  }
}
```

AI가 매출 데이터를 분석하고 Google Docs에 리포트를 자동 생성합니다.

### 4. 발주 요청 이메일 자동 발송

```json
{
  "apiKey": "...",
  "action": "gmail.send",
  "params": {
    "to": "supplier@partner.co.kr",
    "subject": "[자동발주] 위젯A 200개 발주 요청",
    "body": "안녕하세요,\n\n아래 품목의 발주를 요청합니다.\n\n- 품목: 위젯A\n- 수량: 200개\n- 희망 납기: 2026-02-24\n\n감사합니다.",
    "name": "구매팀 AI 어시스턴트"
  }
}
```

재고가 임계값 이하로 떨어지면 AI가 자동으로 공급업체에 발주 이메일을 발송합니다.

## 상태 확인

배포 URL에 GET 요청을 보내면 서버 상태와 지원 액션 목록을 확인할 수 있습니다.

## 라이선스

MIT
